---

---

<div id="bloom-popup-overlay" class="fixed inset-0 bg-black/90 backdrop-blur-sm z-50 flex items-center justify-center p-4 opacity-0 pointer-events-none transition-opacity duration-300">
  <div id="bloom-popup-content" class="relative max-w-md w-full bg-gradient-to-br from-stone-900 via-amber-950 to-stone-900 rounded-2xl shadow-2xl transform scale-95 transition-transform duration-300 overflow-hidden border-2 border-amber-700/40">

    <!-- Classical painting texture overlay -->
    <div class="absolute inset-0 opacity-20 pointer-events-none bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-amber-900/30 via-transparent to-stone-900/50"></div>

    <!-- Subtle floral decoration corners -->
    <div class="absolute inset-0 opacity-10 pointer-events-none">
      <div class="absolute top-0 left-0 w-24 h-24 bg-rose-600 rounded-full filter blur-3xl animate-glow"></div>
      <div class="absolute top-0 right-0 w-24 h-24 bg-amber-600 rounded-full filter blur-3xl animate-glow animation-delay-2000"></div>
      <div class="absolute bottom-0 left-0 w-24 h-24 bg-orange-600 rounded-full filter blur-3xl animate-glow animation-delay-4000"></div>
      <div class="absolute bottom-0 right-0 w-24 h-24 bg-pink-600 rounded-full filter blur-3xl animate-glow animation-delay-6000"></div>
    </div>

    <!-- Close button -->
    <button
      id="bloom-popup-close"
      class="absolute top-3 right-3 z-50 w-8 h-8 flex items-center justify-center bg-amber-100/90 hover:bg-amber-100 rounded-full shadow-lg transition-all duration-300 hover:scale-110 cursor-pointer border border-amber-700/30"
      aria-label="Close popup"
      type="button"
    >
      <svg class="w-5 h-5 text-amber-900 pointer-events-none" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>

    <!-- Content -->
    <div class="relative z-10 p-4 md:p-6">
      <!-- Event image -->
      <div class="mb-4">
        <img
          src="/future-in-bloom/cover.jpg"
          alt="Future in Bloom PT1 - Robotics Event"
          class="w-full h-auto rounded-lg shadow-2xl border-2 border-amber-700/40"
        />
      </div>

      <!-- Event info -->
      <div class="text-center mb-4">
        <h2 class="text-2xl md:text-3xl font-bold text-amber-100 drop-shadow-lg mb-2 tracking-wide" style="font-family: serif;">
          FUTURE IN BLOOM
        </h2>
        <div class="w-24 h-px bg-gradient-to-r from-transparent via-amber-600 to-transparent mx-auto mb-2"></div>
        <p class="text-xs uppercase tracking-widest text-amber-200/80 mb-3" style="font-family: serif;">Part One</p>
        <p class="text-base md:text-lg font-semibold text-amber-100/90 drop-shadow-md mb-1">
          18 October 2025
        </p>
        <p class="text-sm text-amber-200/80">
          10:00 AM - 8:00 PM
        </p>
        <p class="text-sm text-amber-200/80">
          Commons Hub Brussels
        </p>
        <p class="text-xs text-amber-300/70 mt-2 italic px-2">
          A co-working make-athon celebrating embodied AI
        </p>
      </div>

      <!-- CTA buttons -->
      <div class="flex flex-col gap-3 justify-center">
        <a
          href="https://luma.com/vl1ksuok"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center justify-center px-6 py-3 text-base font-bold text-stone-900 bg-gradient-to-r from-amber-300 to-amber-400 rounded-lg hover:from-amber-200 hover:to-amber-300 transition-all duration-300 hover:scale-105 shadow-lg hover:shadow-xl border border-amber-600/50"
        >
          Register on Luma
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </a>
        <button
          id="bloom-popup-dismiss"
          class="inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-amber-100 bg-stone-800/60 backdrop-blur-sm rounded-lg hover:bg-stone-800/80 transition-all duration-300 border border-amber-700/40"
        >
          Maybe Later
        </button>
      </div>
    </div>

    <!-- Floating rose petals -->
    <div class="absolute inset-0 pointer-events-none overflow-hidden hidden md:block">
      {[...Array(4)].map((_, i) => (
        <div
          class="absolute text-xl opacity-30 animate-float-petal"
          style={`
            left: ${Math.random() * 100}%;
            top: ${Math.random() * 100}%;
            animation-delay: ${Math.random() * 5}s;
            animation-duration: ${8 + Math.random() * 4}s;
          `}
        >
          🌹
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  @keyframes glow {
    0%, 100% {
      opacity: 0.05;
      transform: scale(1);
    }
    50% {
      opacity: 0.15;
      transform: scale(1.1);
    }
  }

  .animate-glow {
    animation: glow 8s ease-in-out infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  .animation-delay-6000 {
    animation-delay: 6s;
  }

  @keyframes float-petal {
    0% {
      transform: translateY(0) rotate(0deg) translateX(0);
      opacity: 0.2;
    }
    25% {
      transform: translateY(-15px) rotate(90deg) translateX(10px);
      opacity: 0.3;
    }
    50% {
      transform: translateY(-25px) rotate(180deg) translateX(-5px);
      opacity: 0.25;
    }
    75% {
      transform: translateY(-15px) rotate(270deg) translateX(15px);
      opacity: 0.3;
    }
    100% {
      transform: translateY(0) rotate(360deg) translateX(0);
      opacity: 0.2;
    }
  }

  .animate-float-petal {
    animation: float-petal 12s ease-in-out infinite;
  }

  #bloom-popup-overlay.show {
    opacity: 1;
    pointer-events: auto;
  }

  #bloom-popup-overlay.show #bloom-popup-content {
    transform: scale(1);
  }

  /* Mobile optimization */
  @media (max-width: 640px) {
    #bloom-popup-content {
      max-height: 90vh;
      overflow-y: auto;
    }
  }
</style>

<script>
  const POPUP_KEY = 'future_bloom_popup_shown';
  const POPUP_DELAY = 0;

  function showPopup() {
    const overlay = document.getElementById('bloom-popup-overlay');
    if (overlay) {
      setTimeout(() => {
        overlay.classList.add('show');
      }, 100);
    }
  }

  function hidePopup() {
    const overlay = document.getElementById('bloom-popup-overlay');
    if (overlay) {
      overlay.classList.remove('show');
      sessionStorage.setItem(POPUP_KEY, 'true');
    }
  }

  document.addEventListener('DOMContentLoaded', () => {
    const alreadyShown = sessionStorage.getItem(POPUP_KEY);

    if (!alreadyShown) {
      setTimeout(() => {
        showPopup();
      }, POPUP_DELAY);
    }

    const closeButton = document.getElementById('bloom-popup-close');
    if (closeButton) {
      closeButton.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        hidePopup();
      });
    }

    const dismissButton = document.getElementById('bloom-popup-dismiss');
    if (dismissButton) {
      dismissButton.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        hidePopup();
      });
    }

    const overlay = document.getElementById('bloom-popup-overlay');
    if (overlay) {
      overlay.addEventListener('click', (e) => {
        if (e.target === overlay) {
          hidePopup();
        }
      });
    }

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') {
        const overlay = document.getElementById('bloom-popup-overlay');
        if (overlay && overlay.classList.contains('show')) {
          hidePopup();
        }
      }
    });
  });
</script>
